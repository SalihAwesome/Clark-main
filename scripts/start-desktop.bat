@echo off
REM [Desktop app — no longer bundled. The Electron shell was removed.]
echo.
echo  The desktop Electron app has been removed.
echo  Clark now runs as a web app only: backend (port 8008) + frontend (port 3000).
echo.
pause
