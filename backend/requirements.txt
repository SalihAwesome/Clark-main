# NOTE: most entries use lower-bound pins (>=) so this installs cleanly on newer
# Pythons (e.g. 3.14), which need recent wheels. Playwright is pinned exactly (==)
# to stay in sync with the Docker base image. Tested on Python 3.12–3.14.
fastapi>=0.115.6
uvicorn>=0.34.0          # plain uvicorn: --reload falls back to StatReload (no compiled httptools/watchfiles needed on 3.14)
httpx>=0.28.1
python-dotenv>=1.0.1
pydantic>=2.11.7         # 2.11.x pulls pydantic-core with cp314 wheels (2.10.x did not)
python-multipart>=0.0.20
defusedxml>=0.7.1          # safe XML parsing (used in tools.py)
playwright==1.61.0         # pin to match the Docker base image (mcr.microsoft.com/playwright/python)
duckduckgo-search>=8.0.0   # web search via DuckDuckGo API
